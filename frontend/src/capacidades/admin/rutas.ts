/**
 * Rutas de la capacidad administrativa.
 *
 * La dirección es la que fija la definición del producto: el área administrativa vive en `/admin`,
 * dentro del mismo frontend de Moica.
 */

export const RUTA_ADMIN = '/admin';
