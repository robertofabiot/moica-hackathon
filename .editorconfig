# EditorConfig ayuda a los desarrolladores a definir y mantener consistencia en el estilo de código
# https://editorconfig.org

root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

# Frontend (React/TypeScript) y Archivos Web
[*.{js,jsx,ts,tsx,css,json,html,md,yml,yaml}]
indent_style = space
indent_size = 2

# Backend (Java/Spring Boot)
# 2 espacios para coincidir con Google Java Format, que es lo que aplica
# Spotless. Con 4, el editor y `spotless:check` se contradirian en cada archivo.
[*.java]
indent_style = space
indent_size = 2

# Configuracion de Maven y recursos del backend
[*.{xml,properties}]
indent_style = space
indent_size = 2

# En Markdown, dos espacios finales significan salto de linea: no se recortan
[*.md]
trim_trailing_whitespace = false

[Makefile]
indent_style = tab
